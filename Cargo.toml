[workspace]
members = ["src-tauri", "crates/rtvt-engine"]
resolver = "2"

# Optimize dependencies in dev mode — critical for whisper.cpp / CTranslate2 performance.
# Without this, inference runs at opt-level=0 which is 5-10x slower.
[profile.dev.package."*"]
opt-level = 2
